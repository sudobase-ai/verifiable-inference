
# The enclave app listens on this port.  The same port is used on the host for
# forwarding to the enclave.
DEFAULT_APP_SERVER_PORT=5001

# VSOCK -> EXTERNAL forwarder listens on this port
DEFAULT_VSOCK_PROXY_PORT=5002

DEFAULT_VSOCK_PROXY_ADDR=3

DEFAULT_REMOTE_HOST = "localhost"

DEFAULT_REMOTE_PORT = 443
